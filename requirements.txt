# PyTorch is CPU-only here. Install it FIRST from the CPU wheel index,
# otherwise pip pulls the ~2.5 GB CUDA build for no benefit:
#
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
#
# then:  pip install -r requirements.txt

torch==2.8.0
numpy==2.2.6
opencv-python==4.12.0.88
scikit-learn==1.7.2
matplotlib==3.10.6
pillow==11.1.0
pandas==2.3.2
tqdm==4.67.1
